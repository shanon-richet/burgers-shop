import pool from '../db.mjs'

const payWithBancontact = (async(req, res) => {
    const session = await stripe
})

const payWithPaypal = (async(req, res) => {
    
})

const payWithCreditCard = (async(req, res) => {
})

const payWithCash = (async(req, res) => {
})


export {payWithBancontact, payWithPaypal, payWithCreditCard, payWithCash}